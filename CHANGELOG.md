# 更新日志

## [0.3.5] - 2026-09-13

### 修复
- **群级模式覆盖导致"改了全局默认却不生效"（关键）**：早期版本在群首次登记时会把**当时的全局模式**复制进群配置，
  于是之后在管理台把「默认模式」改成 `standard` 时，已登记的群仍被旧值 `lenient` 覆盖 →
  命中违规只警告、不撤回不禁言（生产实测：设置已是 standard + dry-run 关闭，动作仍被 lenient 过滤）。
  现在**建群不再复制全局模式**（空值 = 跟随全局），新建/后续改动都会跟随全局默认。
- **"未执行动作"日志补上模式来源**：现在输出
  `以下动作未执行：['recall']（模式=lenient，群级覆盖，dry_run=False）`，一眼能看出是全局还是群级设置导致。

### 新增
- **`审核模式 跟随` 指令**：清除该群的模式覆盖，回到跟随全局；`审核状态` 会显示
  "宽松（群级覆盖）" / "标准（跟随全局）"这样的来源标注。
- **管理台「群管理」表格新增每群模式下拉**（跟随全局 / strict / standard / lenient / log_only），
  新增后端接口 `POST /{plugin}/groups/mode`（mode 传空串即跟随全局）。

### 测试
- 新增 2 条：群级覆盖优先且「跟随」能清除覆盖、新建群不复制全局模式（历史 bug 回归）；全量 **110 passed**，ruff 全绿。

## [0.3.4] - 2026-09-13

### 新增
- **`dry-run 关闭 / dry-run 开启 / dry-run` 指令**：群内即可查看与切换"演练 / 实际处置"，不必再进管理台
  （仍受群管权限限制）。管理台「策略」页保持原有开关。

### 修复
- **违规动作被拦下时群里完全没有反馈**：`standard` 模式下矩阵是"撤回 + 禁言"，若 dry-run 打开或机器人不是群管理员，
  这两个动作都会被拦掉，且不会补警告 —— 群内看不到任何反应。现在只要违规且有破坏性动作被拦，就会**兜底补一条警告**，
  并在警告文案里说明原因（"（宽松模式只警告不撤回/禁言）"／"（dry-run 只警告、不实际处置）"）。
- **"为什么没撤回"现在可自证**：动作被模式 / dry-run 拦下时，插件日志会输出
  「以下动作未执行：['recall']（模式=lenient，dry_run=True）」，配合管理台「日志中心 → 审核事件 / 动作执行」即可定位。
- 菜单与 `docs/CONFIG.md` 新增「让处置真正生效」一节，说明模式 / dry-run / 能力三层保护与切换方式。

### 测试
- 新增 3 条链路测试：lenient 下不撤回但警告文案含原因、standard + dry-run 下兜底警告含 dry-run 说明、`dry-run` 指令切换与状态查询；
  全量 **108 passed**，ruff 全绿。

## [0.3.3] - 2026-09-13

### 修复
- **审核链路在真实运行时直接抛异常（关键）**：`main.py` 的"入群时长"判定调用了 `PluginStore.member_first_seen()`，
  而该方法从未实现 —— 于是**每条群消息一进审核链路就抛 `AttributeError`**，异常被最外层兜住只写了日志，
  群内既不警告、审计库也没有记录，用户侧表现就是"发广告没反应"。现已实现该方法（首次见到成员时记录 `first_seen`）。
- **dry-run 期间连警告都不发**：dry-run 原本拦截**所有**动作，包括非破坏性的警告与上报，导致开启 dry-run 时群里完全看不到任何反馈。
  现在 dry-run 只拦截破坏性动作（撤回 / 禁言 / 拉黑 / 移除），警告与上报照常发送；新增配置 `dry_run_warn`（默认开）用于彻底静默。

### 新增
- **审核链路集成测试** `tests/test_moderation_chain.py`（9 条）：用假事件跑真实链路（规则 → LLM → 处置 → 审计 → 去重 → 豁免），
  覆盖 lenient + dry-run 仍发警告、dry_run_warn 关闭后静默、standard 模式真实撤回+禁言并写台账、dry-run 拦截破坏性动作、
  管理员豁免、LLM 失败降级、正常消息无动作、重复消息只处理一次、first_seen 记录。**这类测试正是用来兜住"只在真实路径才暴露的错"的**。

### 优化
- 管理台 dry-run 文案与实际行为对齐（"不做撤回/禁言等实际处置，仍会发警告"），并新增 `dry_run_warn` 开关。

### 测试
- 全量 **105 passed**；`ruff format` 与 `ruff check` 全绿。

## [0.3.2] - 2026-09-13

### 修复
- **WebUI 按钮点了没反应（关键）**：插件页面运行在 sandbox iframe 中（仅 `allow-scripts / allow-forms / allow-downloads`），
  浏览器会**直接忽略**原生 `window.confirm / alert / prompt` 并返回 false/undefined —— 于是所有带二次确认的操作
  （启用审核、移除群记录、清空日志、移除成员、黑名单、白名单…）点击后静默无响应。
  现已改为自绘模态框（复用同一套 CSS 变量，支持 Esc / Enter / 点击遮罩关闭），11 处调用全部替换，并补上模态框样式。
- **QQ 指令没反应的两个成因**：
  1. 指令执行过程中抛异常时，原来只写日志**不回话**，用户侧看到的就是"没反应"；现在任何异常都会回复
     "指令执行失败：<类型>: <原因>"，同时写完整堆栈到插件日志。
  2. 在**私聊**里发送群管理指令原来完全没有响应（插件只监听了群消息）；现新增私聊 handler，
     明确回复"群管理指令只作用于群聊"，并列出私聊可用的指令。

### 优化
- **"为什么没生效"可查**：收到指令、开始启用审核、启用被拒绝（未开启接收全部消息 / 无法读取群内状态）
  都会写入插件日志（INFO / WARNING），配合管理台「日志中心」的能力受限记录，排查不再靠猜。

### 测试
- 新增 `tests/test_moderation_toggle.py`（7 条）：启用成功、非群管理员给降级提示、未开启全量消息被拒绝并返回指引、
  群状态不可用被拒绝、`allow_without_full_msg` 逃生舱、停用清空暂停原因、私聊与群聊 handler 均已注册；全量 **96 passed**。


## [0.3.1] - 2026-09-13

### 新增
- **可视化选择审核模型**：管理台「策略 → 运行参数」新增「审核模型」下拉（选项来自 AstrBot 已配置的对话模型），
  新增配置项 `llm_provider_id`；留空表示**跟随会话默认模型**（沿用 `get_current_chat_provider_id(umo)`）。
- **总览显示当前生效模型**：总览统计卡展示最近一次审核实际使用的模型，便于确认是否走的是预期模型。
- **模型失效自动回退**：配置的模型被删除/改名时，审核链路自动回退到会话默认模型，并在插件日志给出 warning，
  不会因为一个无效配置就让审核整体不可用。

### 修复
- **提示词与刷屏阈值保存无效**：WebUI 策略页会写入 `prompt_system` / `prompt_user` / `flood_threshold`，
  但它们未在默认配置中声明，`normalize_settings` 会把未知键静默丢弃 —— 用户改了提示词其实并未生效。
- **配置归一化对脏数据会抛异常**：数值项用 `int(当前值)` 兜底，手填字符串等非法值会直接抛 `ValueError` 导致保存失败；
  改为用内置默认值兜底并钳制到合法区间。
- **审计里的 model 字段恒为空**：`mod_events.provider_id` 之前读的是一个并不存在的配置键，现改为记录实际使用的模型 ID。

### 测试
- 新增 `choose_provider_id` 分支测试（未配置 / 已配置可用 / 已配置失效 / 无法枚举）与配置项持久化回归；
  全量 **89 passed**，`ruff format` 与 `ruff check` 全绿。


## [0.3.0] - 2026-09-13

### 新增
- **入群申请智能审批**（`src/join_review.py`）：按群轮询 `join_request_list`（游标持久化 + 数据库幂等去重，重启不重复审批）；
  判定链为「本地/平台黑名单 → 机器人账号 → 平台风险提示 → 信任邀请人 → LLM 判定」，输出 approve/decline + 置信度 + 理由（拒绝理由回传平台）。
- **四种审批模式** `off / strict / standard / human`：strict 只放行高置信通过、其余自动拒绝；standard 高置信自动、低置信转人工；human 全部人工（WebUI 或群指令处理）。
- **官方「入群自动审批策略」透明化**（`src/policy.py`）：策略列表查询、启停、触发全量扫描、白名单批量增删，
  并检测「官方策略 + 插件自动审批」同时生效的冲突（管理台顶部横幅提示）。
- **成员管理**：指令与 WebUI 双入口的手动禁言/解禁；本地禁言台账每 5 分钟与平台 `restrict_chat_setting` 对账
  （平台已解除的自动关闭）；成员查询（本地昵称缓存 + 内邀成员接口）；黑名单平台/本地双轨；批量移除（二次确认）。
- **指令扩展**：`入群审核`、`入群申请`、`入群通过`、`入群拒绝`、`黑名单`、`禁言`、`解禁`、`撤回`、`申诉`。
- **WebUI 新增两个视图**：「成员与禁言」（台账 / 解禁 / 对账 / 成员查询 / 黑名单双栏 / 移除成员）、
  「入群审批」（待审列表 / 一键通过拒绝 / 立即拉取 / 官方策略与白名单）；8 个视图全部可用。

### 测试
- 新增 `tests/test_join_review.py`（8 条）与 `tests/test_policy.py`（4 条）：硬规则短路、阈值与模式分支、
  自动审批与转人工、审批失败留痕、幂等去重、策略缓存与冲突检测；全量 **80 passed**。

## [0.2.0] - 2026-09-13

### 新增
- **本地规则层**（`src/rules.py`）：硬规则（命中即按规则动作处置）与软规则（提升关注度）两栏，
  支持关键词与正则、作用域（全局 / 指定群）、启用开关；内置外链、联系方式、重复字符、超长文本检测，
  以及按成员统计的 60 秒刷屏判定。
- **LLM 审核引擎**（`src/moderator.py`）：内置审核提示词，要求严格 JSON 输出（verdict / category / severity / confidence / reason / suggested_action）；
  平衡括号提取 + 字段钳制 + 置信度门槛（低于门槛降级为「需人工复核」且不处置）；
  相同文本结果缓存、送审采样率、单群 QPM、全局并发、每日预算、连续失败熔断（默认 300 秒）。
- **处置执行器**（`src/actions.py`）：按 verdict × severity 矩阵执行警告 / 撤回 / 禁言 / 拉黑 / 移除 / 上报；
  模式约束（log_only 不执行任何动作、lenient 只保留警告与上报并强制补一条警告）；
  能力降级（机器人非群管理员时跳过撤回与禁言并记录原因）；同一消息幂等（平台重复推送不重复处置）；
  禁言时长阶梯与 24 小时内重复违规累加（≤3 倍、≤30 天）；处置结果全部写入 SQLite。
- **申诉机制**：成员回复被处置消息并发送「申诉 <理由>」，事件被标记申诉并通知管理员，管理台可按「已申诉」筛选。
- **WebUI 新增两个视图**：「策略」（运行参数、处置矩阵、提示词编辑、dry-run 试跑）与「关键词」（规则增删改、启用开关、作用域、命中测试）。
- **新配置项**：`prompt_system` / `prompt_user`（留空用内置默认）、`flood_threshold`（刷屏阈值）。

### 修复
- **采样率 0 被吞掉**：`sample_rate=0` 时因 `or` 短路被当成 1.0，导致配置为「不采样」反而全量送审；已改为显式判空。
- **默认处置矩阵改为「标准档」**（1-2 警告、3 警告+禁言、4 撤回+禁言、5 撤回+禁言+上报）；
  首次安装仍以 `mode=lenient` 兜底，因此「安装即温和」与「切到标准档即完整处置」同时成立。

### 实现说明
- 审核链路：群消息 → 登记与豁免判定 → 本地规则 →（满足送审条件时）LLM 判定 → 处置矩阵 → 审计。
  解析失败、模型异常、熔断期间一律降级为「需复核」且不执行任何处置，避免误伤。
- 该 handler 命中会把事件标记为 `is_wake`，但**不会**触发聊天 LLM（AstrBot 仅在 `is_at_or_wake_command` 为真时调用）。

### 测试
- 新增 `tests/test_rules.py`（6 条）、`tests/test_moderator.py`（8 条）、`tests/test_actions.py`（7 条）：
  规则命中与作用域、内置检测器、刷屏窗口；JSON 解析边界、阈值降级、缓存、熔断、预算与采样；
  动作规划、lenient/log_only 过滤、能力降级、幂等。全量 **60 passed**。

## [0.1.0] - 2026-09-13

### 新增
- **插件骨架**：Star 入口、服务装配、生命周期管理、KV 配置存储、后台任务调度（周期任务 + 异常隔离 + 手动触发）、插件专属日志通道。
- **QQ 群管理 API 客户端**（`src/api_client.py`）：覆盖群信息、机器人群内状态、入群申请列表/审批、禁言读写、撤回、
  成员列表/详情、批量移除、黑名单，以及官方入群自动审批策略 6 个接口；
  令牌桶限频（全局 + 单群）、指数退避重试、`err_code` → 语义错误映射、botpy 异常翻译、
  **dry-run 在客户端层拦截写操作**、所有调用写入审计库（含 trace_id / 耗时 / 重试次数）。
- **能力探测**（`probe`）：只用只读接口逐项探测「群信息 / 群状态 / 是否管理员 / 接收全部消息 / 撤回 / 禁言 /
  入群审批 / 成员列表 / 黑名单」，无只读接口的能力（批量移除）标记为「按需尝试」。
- **SQLite 审计库**（`src/audit.py`）：9 张表（审核事件 / 处置动作 / 入群申请 / API 调用 / 能力受限 / 禁言台账 / 日统计 / 元信息）；
  异步批量写队列（队列满丢弃并计数，绝不阻塞事件循环）、WAL、保留期自动裁剪、VACUUM、一致性备份、CSV/JSON 导出。
- **指令**：`群管理菜单`、`群信息`、`审核状态`、`群管理自检`、`群管理配置`。
- **审核启用前置校验**：必须已开启「接收全部消息」，否则拒绝启用并给出手机 QQ 操作指引；后台每 30 分钟复查，能力丢失自动暂停审核并告警。
- **WebUI 管理台**（`pages/manage/`）：单页 SPA + hash 路由，首版提供「总览」「群管理」「日志中心」
  （审核事件 / 动作执行 / API 调用 / 能力受限，含筛选、分页、导出、清空）「工具」（能力自检、审计库维护与备份、SSE 实时事件、指令速查）。

### 修复
- **平台 4xx 权限类错误误判与重试风暴**：真实平台验证发现 botpy 在 HTTP 4xx 时只透出 message（`err_code` 丢失），
  未知状态码统一包装成 `ServerError`，导致把「无权限」误判为服务端错误并对 400 重试 3 次。
  现改为：支持 `code` 字段（QQ 把业务码放在 `code` 中，`err_code` 是另一套编号）、
  按错误文案识别白名单/管理员/撤回时限等语义、权限类错误不重试。

### 实现说明
- 审计库使用标准库 `sqlite3` + `asyncio.to_thread`（而非 aiosqlite），插件因此**零第三方依赖**，
  避免与 AstrBot 环境的版本冲突；`requirements.txt` 仅作说明。
- 指令不走 `@filter.command`，而是在群消息 handler 内做全匹配分派 —— 群开启「接收全部消息」后，
  非 @ 场景下指令依然可用，普通聊天不会误触发。

### 测试
- 47 个单元与契约测试：限频、重试策略、错误码映射、dry-run、能力探测降级、审计读写与裁剪、
  配置归一化、成员与角色缓存、Web 路由契约（带插件名前缀）、metadata 契约。
- 真机验证：AstrBot v4.27.3 加载成功、SQLite 建表完成、5 个后台任务运行、运行日志 0 错误；
  真实平台只读探测确认该机器人「群信息/群状态」白名单可用，成员管理 5 个接口返回 11253（内邀未开放）。

