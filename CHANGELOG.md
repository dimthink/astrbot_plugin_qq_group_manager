# 更新日志

## v0.3.0（M3：入群审批与成员管理）

- **入群申请智能审批**（`src/join_review.py`）：按群轮询 `join_request_list`（游标持久化、数据库幂等去重）；
  判定链 = 本地/平台黑名单 → 机器人账号 → 平台风险提示（`top_tips` 直接拒绝并拉黑）→ 信任邀请人 → LLM 判定（approve/decline + 置信度 + 理由）；
  四种模式 off / strict / standard / human，低置信自动转人工，人工可通过 WebUI 或群指令处理。
- **官方入群自动审批策略透明化**（`src/policy.py`）：策略列表查询、启停、全量扫描触发、白名单批量维护，
  并检测「官方策略 + 插件自动审批」同时生效的冲突。
- **成员管理**：手动禁言/解禁（指令与 WebUI 双入口）、本地禁言台账每 5 分钟与平台对账、成员查询（本地缓存 + 内邀接口）、
  黑名单平台/本地双轨（平台不可用自动回落本地）、批量移除（二次确认 + 失败留痕）。
- **指令扩展**：`入群审核`、`入群申请`、`入群通过`、`入群拒绝`、`黑名单`、`禁言`、`解禁`、`撤回`、`申诉`。
- **WebUI**：新增「成员与禁言」「入群审批」视图，8 个视图全部可用。
- **测试**：新增 join_review / policy 测试，合计 80 个单元与契约测试。

## v0.2.0（M2：LLM 消息审核与处置）

- **本地规则层**（`src/rules.py`）：硬规则（命中即处置）/ 软规则（提升关注）两栏，支持关键词与正则、作用域（全局/单群）、
  启用开关；内置外链、联系方式、重复字符、超长文本检测与 60 秒刷屏统计。
- **LLM 审核引擎**（`src/moderator.py`）：内置审核提示词（严格 JSON：verdict/category/severity/confidence/reason/suggested_action）；
  平衡括号 JSON 提取、字段钳制、置信度门槛、相同文本缓存、采样率、单群 QPM、全局并发、日预算、连续失败熔断；解析失败一律降级为「需复核」。
- **处置执行器**（`src/actions.py`）：按 verdict × severity 矩阵执行警告 / 撤回 / 禁言 / 拉黑 / 移除 / 上报；
  模式约束（log_only 不执行、lenient 只警告与上报）、能力降级（非管理员时跳过撤回与禁言并记录原因）、
  同一消息幂等、禁言时长阶梯与重复违规累加、禁言台账写入、审核事件与动作全部落 SQLite。
- **申诉机制**：成员回复被处置消息并发送「申诉 <理由>」，事件标记申诉并通知管理员。
- **WebUI**：新增「策略」视图（运行参数、处置矩阵、提示词、试跑）与「关键词」视图（规则 CRUD、命中测试）；
  日志中心的审核事件/动作执行两类日志在 M2 后开始产生。
- **修复**：采样率配置为 0 时被 `or` 吞掉导致全量送审的问题；默认处置矩阵调整为「标准档」（首次安装仍以 lenient 模式兜底）。
- **测试**：新增 rules / moderator / actions 测试，合计 60 个。

## v0.1.0（M1：骨架与群管理基础）

首个版本，交付：

- **插件骨架**：Star 入口、KV 配置存储、后台任务调度、日志；
- **QQ 群管理 API 客户端**（`src/api_client.py`）
  - 覆盖群信息、机器人群内状态、入群申请列表/审批、禁言读写、撤回、成员列表/详情、批量移除、黑名单；
  - 令牌桶限频（全局 + 单群）、指数退避重试、`err_code` → 语义错误映射、botpy 异常翻译；
  - dry-run 拦截写操作；所有调用写入审计库（含 trace_id、耗时、重试次数）；
  - 能力探测（`probe`）：只读接口逐项探测，写类能力标记为"按需尝试"。
- **SQLite 审计库**（`src/audit.py`）
  - 9 张表：审核事件、处置动作、入群申请、API 调用、能力受限、禁言台账、日统计、元信息；
  - 异步批量写队列（队列满丢弃并计数，绝不阻塞事件循环）、WAL、保留期裁剪、VACUUM、一致性备份、CSV/JSON 导出。
- **指令**：`群管理菜单`、`群信息`、`审核状态`、`群管理自检`、`群管理配置`。
- **WebUI 管理台**（`pages/manage/`）：总览、群管理、日志中心（审核事件 / 动作执行 / API 调用 / 能力受限，含筛选、分页、导出、清空）、
  工具（能力自检、审计库维护与备份、SSE 实时事件、指令速查、配置摘要）；策略 / 关键词 / 成员与禁言 / 入群审批 为 M2、M3 占位视图。
- **审核启用前置校验**：必须已开启「接收全部消息」，否则拒绝启用并给出操作指引；后台周期复查，能力丢失自动暂停。
- **测试**：47 个单元与契约测试（限频、重试、错误码映射、dry-run、能力探测、审计读写、配置归一化、Web 路由契约）。
