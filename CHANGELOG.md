# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更 · ⚠️ 升级注意

---

## [0.5.0] - 2026-09-13

> 反垃圾识别能力大幅增强：形近字、拆字、繁体、谐音、黑话写法都能识别。实测真实语料 65 条正样本全部命中，38 条正常消息零误报。

### ✨ 新增
- **黑话 / 暗语词表**：懂的都懂、你懂的、自取、上车、车牌、防失联、备用群、内部群、稳赚不赔、全网首发、仅限今天、秒发、诚信、代找、朋友圈、一包烟钱 …；弱标记（深夜、睡不着、寂寞、老司机）单独出现只加分不送审，**短消息 + 强标记**才判为暗语引流。
- **广告模板扩充到 9 条**：新增「黑话引流」「深夜福利引流」「盗版资源贩卖」「召唤式引流」（连同原有的拉群引流 / 私聊引流 / 兼职刷单 / 赌博引流 / 涉黄引流）；词表拆分为**引导动作词 / 诱饵资源词 / 黑话标记词**三类，分别为 41 / 39 / 45 条。
- **拆字与繁体识别**：多字混淆短语（君羊→群、看爿→看片）与繁体字表（機→机、發→发、車→车、資→资、領→领、號→号 …），内置形近字表扩充到 100+ 条。
- **联系方式变体识别**：`V：abc123`、`裙号 123456` 等写法。
- **风险分新增信号**：黑话 +40、多处黑话 +60、诱饵词 ≥3 个 +60（2 个 +40）、短消息暗语 +25。

### 🐛 修复
- 模板要求各分组命中**不同**的词，避免"车牌号我记不清了"这类正常发言自匹配而误报。
- 调整易误伤词：移除动词"下载"、标记词"更新了 / 求带"、强标记"车牌"（避免"公告我更新了""合集下载完了"被误判）。

### ⚠️ 升级注意
- 判定更严格、也更容易送审；若此前已关闭 dry-run，建议先切回观察一天。

---

## [0.4.2] - 2026-09-13

> 修复策略页"勾选后保存又变回默认"的问题。

### 🐛 修复
- 送审条件新增的「疑似联系方式 / 命中广告模板 / 含图片」未被后端白名单接受，保存时被静默丢弃 —— 已补齐。
- 保存成功后的提示会带上**实际生效的送审条件**，便于确认后端存了什么。

---

## [0.4.1] - 2026-09-13

> 修复拼音写法识别，并收紧送审条件语义：闲聊与普通链接不再消耗 token。

### 🐛 修复
- 拼音视图此前会丢弃非汉字内容，导致 `jiaqun ling ziliao` 这类纯拼音写法无法匹配。
- 规则的归一化匹配新增拼音兜底：`ｊｉａ群领资料`、`jiaqun ling ziliao` 均可命中 `加群领资料`。

### ⚙️ 变更
- `rule_hit` 只认"规则 / 模板命中"；链接、联系方式、长数字等内置信号改由 `risk>=N` 或独立条件参与判断，"闲聊 + 偶尔发链接"不再送审。
- 权重调优：联系方式 60、8 位以上长数字 60、图片 60（各自单独即达阈值）。

---

## [0.4.0] - 2026-09-13

> 引入变体识别体系：从"关键词匹配"升级为"归一化 + 模板 + 风险分"。

### ✨ 新增
- **文本归一化**：每条消息生成原文 / compact（去全角、去符号、去零宽）/ 骨架（形近字映射）/ 拼音视图，规则多视图同时匹配。
- **规则类型扩展**：`normalized`（新规则默认）/ `literal` / `regex` / `fuzzy`（编辑距离）/ `pinyin`（同音，需 pypinyin）。
- **风险分与送审条件**：本地信号累加 0–100，送审条件支持 `risk>=N`、`has_image`、`has_contact`、`ad_template`。
- **同文案多号刷屏检测**：同一骨架文本在窗口内被多个成员发送即计入风险分。
- **管理台「规则增强」视图**：广告模板与形近字表可视化维护 + **判定测试**（展示归一化视图、命中依据、风险分明细与是否送审）。
- **回归语料与评分机制**：正样本送审率、负样本误报率成为可量化指标。

### ⚙️ 变更
- 默认送审条件由 `rule_hit` 改为 `rule_hit + risk>=60`。
- 变体 / 模板命中**只送审**，是否违规交由 LLM 判定，处置按判定到的类别与严重度执行。

---

## [0.3.7] - 2026-09-13

### ✨ 新增
- **图片送审**（可选，默认关闭）：不送 / 仅图文混排 / 纯图片也送，图片以多模态交给同一个审核模型。

### 🐛 修复
- 此前纯图片消息会被直接跳过，图文混排也只审文字。

---

## [0.3.6] - 2026-09-13

### 🐛 修复
- **管理台保存关键词不生效**：保存路径绕过了规则引擎热更新，新加的动作要重载插件才生效（"撤回生效了、禁言没生效"的根因）。
- 审核判定日志现在同时打印"实际动作 / 计划动作 / 被拦下的动作"，一眼分清是规则没配还是被模式拦下。

---

## [0.3.5] - 2026-09-13

### 🐛 修复
- **群级模式覆盖导致全局改动不生效**：新建群记录不再复制当时的全局模式；历史覆盖可用 `审核模式 跟随` 清除。

### ✨ 新增
- 管理台「群管理」表格支持逐群选择审核模式（跟随全局 / 严格 / 标准 / 宽松 / 仅记录）。

---

## [0.3.4] - 2026-09-13

### ✨ 新增
- `dry-run 开启 / 关闭 / 查看` 指令，群内即可在"演练"与"实际处置"之间切换。

### 🐛 修复
- 违规动作被模式或 dry-run 拦下时群里毫无反馈 —— 现在会兜底补一条警告，并说明原因。

---

## [0.3.3] - 2026-09-13

### 🐛 修复
- **审核链路在真实运行时抛异常**：缺少一个内部方法，导致每条群消息都报错且静默无响应（表现为"发广告没反应"）。已修复并补上整链路集成测试。
- **dry-run 连警告都吞掉**：dry-run 现在只拦截破坏性动作（撤回 / 禁言 / 拉黑 / 移除），警告与上报照常发送；可用 `dry_run_warn` 完全静默。

---

## [0.3.2] - 2026-09-13

### 🐛 修复
- **管理台按钮点了没反应**：插件页面运行在受限 iframe 中，浏览器会忽略原生确认弹窗（直接返回 false），导致所有带二次确认的操作静默失效。已改为页面内自绘弹窗。
- **QQ 指令没反应**：指令执行异常时不再沉默，会回复失败原因；新增私聊提示（群管理指令只作用于群聊）。

---

## [0.3.1] - 2026-09-13

### ✨ 新增
- 管理台可**选择审核模型**（配置 `llm_provider_id`），留空则跟随会话默认模型；模型失效自动回退。

### 🐛 修复
- 提示词与刷屏阈值此前未在默认配置中声明，管理台保存会被静默丢弃。

---

## [0.3.0] - 2026-09-13

> 入群审批与成员管理上线，管理台补齐到 8 个视图。

### ✨ 新增
- **入群申请智能审批**：游标轮询 + 幂等去重；黑名单 / 机器人账号 / 平台风险提示 / 信任邀请人 / LLM 五级判定；`off / strict / standard / human` 四种模式。
- **官方入群策略透明化**：查看、启停、扫描、白名单管理，并与插件审批做冲突检测。
- **成员管理**：手动禁言 / 解禁（指令 + 管理台）、禁言台账与平台对账、黑名单双轨、批量移除、成员查询。
- **管理台新增视图**：「成员与禁言」「入群审批」。

---

## [0.2.0] - 2026-09-13

> LLM 审核与自动处置上线。

### ✨ 新增
- **本地规则层**：硬规则（命中即处置）/ 软规则（提升关注），支持正则与作用域；内置外链、联系方式、重复字符、超长、刷屏检测。
- **LLM 审核引擎**：严格 JSON 输出 + 置信度门槛；结果缓存、采样率、单群限速、并发上限、日预算、失败熔断。
- **处置执行器**：警告 / 撤回 / 禁言 / 拉黑 / 移除 / 上报，按 severity 矩阵执行，支持演练、幂等与能力降级。
- **申诉机制**：成员可对处置结果申诉，管理台可按"已申诉"筛选。
- **管理台新增视图**：「策略」「关键词」。

---

## 早期版本（0.1.0 – 0.1.x）

> 插件骨架、平台能力探测、审计库与管理台首版。

### ✨ 新增
- AstrBot Star 入口、服务装配、生命周期与后台任务调度（配置落盘、能力探测、审计维护、入群轮询、禁言对账）。
- **QQ 群管理 API 客户端**：覆盖群信息、群状态、入群审批、禁言、撤回、成员、黑名单与官方入群策略；令牌桶限频、指数退避重试、错误码语义映射、dry-run 拦截写操作、全量调用审计。
- **能力自检**：只用只读接口逐项探测平台能力，无只读接口的能力标记为"按需尝试"。
- **SQLite 审计库**：审核事件、动作、入群申请、API 调用、能力受限、禁言台账、日统计；异步批量写入、保留期裁剪、备份与导出。
- **管理台首版**：总览 / 群管理 / 日志中心 / 工具。
- **审核启用前置校验**：必须已开启「接收全部消息」，否则拒绝启用并给出手机 QQ 操作指引；后台周期复查，能力丢失自动暂停。

### 🐛 修复
- 平台 4xx 权限类错误被误判为服务端错误并触发重试风暴。
