# 更新日志

## v0.1.0（M1：骨架与群管理基础）

首个版本，交付：

- **插件骨架**：Star 入口、KV 配置存储、后台任务调度、日志；
- **QQ 群管理 API 客户端**（`src/api_client.py`）
  - 覆盖群信息、机器人群内状态、入群申请列表/审批、禁言读写、撤回、成员列表/详情、批量移除、黑名单；
  - 令牌桶限频（全局 + 单群）、指数退避重试、`err_code` → 语义错误映射、botpy 异常翻译；
  - dry-run 拦截写操作；所有调用写入审计库（含 trace_id、耗时、重试次数）；
  - 能力探测（`probe`）：只读接口逐项探测，写类能力标记为"按需尝试"。
- **SQLite 审计库**（`src/audit.py`）
  - 9 张表：审核事件、处置动作、入群申请、API 调用、能力受限、禁言台账、日统计、元信息；
  - 异步批量写队列（队列满丢弃并计数，绝不阻塞事件循环）、WAL、保留期裁剪、VACUUM、一致性备份、CSV/JSON 导出。
- **指令**：`群管理菜单`、`群信息`、`审核状态`、`群管理自检`、`群管理配置`。
- **WebUI 管理台**（`pages/manage/`）：总览、群管理、日志中心（审核事件 / 动作执行 / API 调用 / 能力受限，含筛选、分页、导出、清空）、
  工具（能力自检、审计库维护与备份、SSE 实时事件、指令速查、配置摘要）；策略 / 关键词 / 成员与禁言 / 入群审批 为 M2、M3 占位视图。
- **审核启用前置校验**：必须已开启「接收全部消息」，否则拒绝启用并给出操作指引；后台周期复查，能力丢失自动暂停。
- **测试**：47 个单元与契约测试（限频、重试、错误码映射、dry-run、能力探测、审计读写、配置归一化、Web 路由契约）。
